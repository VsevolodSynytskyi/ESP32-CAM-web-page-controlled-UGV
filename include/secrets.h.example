#pragma once

// Copy this file to include/secrets.h and fill in your details.
// secrets.h is gitignored; this template is not.
//
// Only station mode uses these - handy for bench work, streaming to a laptop
// on your home network. In the field the vehicle runs a SoftAP and reads
// AP_SSID / AP_PASSWORD from config.h instead, so no router is involved.

#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-network-password"
