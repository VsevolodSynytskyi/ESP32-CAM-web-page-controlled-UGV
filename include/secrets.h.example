#pragma once

// Copy this file to include/secrets.h and fill in your details.
// secrets.h is gitignored; this template is not.
//
// Only Stage 1b (station mode, streaming to a laptop on your home network)
// uses these. Stage 3 onward runs a SoftAP and reads AP_SSID / AP_PASSWORD
// from config.h instead, so the vehicle needs no router in the field.

#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-network-password"
